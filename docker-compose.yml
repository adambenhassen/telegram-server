# Local development stack: Postgres, migrations, and the server, in that order.
#
#   cp .env.example .env && chmod 600 .env
#   docker compose up
#
# `docker compose down` keeps both volumes, which is what makes a restart
# uneventful: the same rows, the same RSA identity, and the same auth-key master
# key. `down -v` destroys them, and the next boot hands every client a new server
# fingerprint and a master key none of their stored auth keys open under, so they
# re-handshake.

# The password has exactly one source, .env, and the DSN exactly one definition
# here — migrate and telegramd must never be able to reach different databases.
# The :? form aborts the whole run before a container starts and names the cause;
# without it an unset variable interpolates to empty and the first symptom is a
# confusing Postgres error.
x-dsn: &dsn "postgres://postgres:${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}@postgres:5432/telegram?sslmode=disable"

services:
  # The same image internal/pgtest and the docker CI job start, and the version
  # atlas.hcl names as its dev database.
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: telegram
    # An open port is not readiness: Postgres accepts connections during its own
    # init and then drops them. migrate waits on this instead of racing it.
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d telegram"]
      interval: 2s
      timeout: 5s
      retries: 20
    # No ports: on purpose. The server reaches Postgres by service name on the
    # compose network, and psql is `docker compose exec postgres psql -U postgres
    # telegram` — so publishing 5432 would buy nothing and put the database on
    # every interface of the host.
    volumes:
      - pgdata:/var/lib/postgresql/data

  # Applies the migrations and exits 0. No restart policy: a re-run against an
  # already migrated database is a no-op, so a restart loop could only hide a
  # real failure.
  migrate:
    # Pinned to the version the ariga/setup-atlas steps in ci.yml use, so the
    # stack and CI cannot apply migrations with different atlas versions.
    image: arigaio/atlas:1.2.0-alpine
    # The image entrypoint is already atlas, so the command starts at `migrate`.
    # Not `--env local`: that env's dev database is docker://postgres/16, which
    # would need a Docker daemon inside this container. Three slashes in
    # file:///migrations — an absolute path, because the working directory here
    # is not the repo root.
    command:
      - migrate
      - apply
      - --dir
      - file:///migrations
      - --url
      - *dsn
    volumes:
      - ./migrations:/migrations:ro
    depends_on:
      postgres:
        condition: service_healthy

  telegramd:
    # Built from the repo-root Dockerfile: nothing is published to a registry, so
    # there is no image to pull. The explicit tag is what lets a build done
    # outside compose — CI builds it with a layer cache — be picked up here
    # instead of rebuilt; without it the name would follow the project directory.
    build: .
    image: telegramd:local
    environment:
      TG_POSTGRES_DSN: *dsn
      # Not :? — an empty value is the normal case for this stack and the line
      # below is what makes it safe: the server generates a key into the tgkey
      # volume on first boot and reuses it after. Set it in .env to pin a
      # specific key, and then no file is written. The binary has no default
      # path of its own, so a bare `docker run` with neither variable set still
      # refuses to start rather than silently minting a key nobody kept.
      TG_AUTHKEY_ENC_KEY: ${TG_AUTHKEY_ENC_KEY:-}
      # Interpolated, not hardcoded: the default is the tgkey volume, and an
      # operator who sets the variable in .env gets their path instead. Point it
      # outside /var/lib/telegramd or /var/lib/telegramd-blobs and the server
      # cannot write it — the container root is read_only.
      TG_AUTHKEY_ENC_KEY_FILE: ${TG_AUTHKEY_ENC_KEY_FILE:-/var/lib/telegramd/enc_key.hex}
      # Not :? like the two above: there is a safe default for this stack, and a
      # .env copied before this line existed must still boot into a server you
      # can sign in to. A bare `true` here would take the knob away instead.
      TG_LOG_LOGIN_CODES: ${TG_LOG_LOGIN_CODES:-true}
      # Optional, no default: leave them unset and first boot behaves exactly
      # as before — registration closed, no account seeded. Set them in .env
      # and the server creates the operator account on first start, which is
      # the only way in while TG_REGISTRATION stays closed. The password must
      # be at least 12 bytes; the server refuses to boot otherwise.
      TG_BOOTSTRAP_USERNAME: ${TG_BOOTSTRAP_USERNAME:-}
      TG_BOOTSTRAP_PASSWORD: ${TG_BOOTSTRAP_PASSWORD:-}
      TG_BOOTSTRAP_PASSWORD_FILE: ${TG_BOOTSTRAP_PASSWORD_FILE:-}
    ports:
      # Loopback only. 2443 is unauthenticated at the transport layer, so whoever
      # reaches it feeds chosen bytes into the MTProto decode path before any
      # identity check exists. Docker publishes on 0.0.0.0 by default through a
      # DNAT rule the host firewall never sees, so "2443:2443" would hand this
      # server to the whole network the laptop is on.
      - "127.0.0.1:2443:2443"
    # TG_LISTEN_ADDR and TG_ADVERTISE_ADDR are both deliberately unset. The
    # listen default :2443 binds every interface in the container, and with no
    # advertise address set the DC address derives from it as 127.0.0.1:2443,
    # which is right for this stack: the only client is on the host and arrives
    # through the published port above. A client reached from anywhere else sets
    # TG_ADVERTISE_ADDR to the name it dials.
    #
    # Proven by the docker job in ci.yml, which already boots this image
    # --read-only: the static binary writes nothing but its key files.
    read_only: true
    volumes:
      # The two writable paths, and the only ones. A named volume takes the mountpoint's 0700 and uid
      # 65532 from the image; a bind mount would take the host directory's mode
      # instead — root-owned means the server cannot write its keys and crash
      # loops, 0755 means a live RSA private key and the auth-key master key
      # readable by every local user.
      - tgkey:/var/lib/telegramd
      # Uploaded file bodies, on their own volume: they grow without bound and
      # the key volume must not. Same 0700 uid 65532 inheritance as above.
      - tgblobs:/var/lib/telegramd-blobs
    # store.Open refuses to start against an unmigrated schema
    # (internal/store/store.go:69), so the ordering is load-bearing, not cosmetic.
    depends_on:
      migrate:
        condition: service_completed_successfully

volumes:
  pgdata:
  tgkey:
  tgblobs:
