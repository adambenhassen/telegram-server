plans/
bin/
# TG_RSA_KEY_PATH defaults to ./server_key.pem, so a local run writes a live private key here
*.pem
*.key
.env
.env.*
# Must stay below .env.*, which would otherwise swallow it; the template carries
# no secret values.
!.env.example
